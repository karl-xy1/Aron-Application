import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'aronaiast01.vercel.app',
  appName: 'Aron- Ai Assistant',
  webDir: 'dist'
};

export default config;
